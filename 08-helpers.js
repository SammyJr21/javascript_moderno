const tech = 'Node.js';
const array = ['Node.js', 'React', 'TypeScript'];

const includesString = tech.includes('Node');
const includesArray = array.includes('React');

const startWith = tech.startsWith('No');
const endsWith = tech.endsWith('de');